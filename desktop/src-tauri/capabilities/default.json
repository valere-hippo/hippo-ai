{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window access for the hippo-ai desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "dialog:default",
    "allow-prepare-environment",
    "allow-run-analysis",
    "allow-manage-projects"
  ]
}
